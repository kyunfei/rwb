name: Build & Release

# 触发条件：
# 1. push 到 master → 自动打包测试版（dev）
# 2. 手动触发 → 可选正式版(stable)或测试版(dev)
on:
  push:
    branches:
      - master
  # PR 只跑静态分析门禁，不打包（打包任务见各 job 的 if 条件）
  pull_request:
    branches:
      - master
  workflow_dispatch:
    inputs:
      release_type:
        description: '打包类型'
        required: true
        default: 'dev'
        type: choice
        options:
          - dev
          - stable

permissions:
  contents: write

env:
  FLUTTER_CHANNEL: 'stable'
  FLUTTER_VERSION: '3.44.8'
  JAVA_VERSION: '21'

jobs:
  # ===== 版本号计算（共享）=====
  prepare:
    name: Prepare Version
    # PR 上只跑 analyze 门禁，打包链整条跳过
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
      version_compact: ${{ steps.version.outputs.version_compact }}
      build_number: ${{ steps.version.outputs.build_number }}
      commit_count: ${{ steps.version.outputs.commit_count }}
      build_date: ${{ steps.version.outputs.build_date }}
      release_type: ${{ steps.release_type.outputs.type }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Generate Version
        id: version
        run: |
          YY=$(date -u +%y)
          MMDD=$(date -u +%m%d)
          TODAY=$(date -u +%Y-%m-%d)
          COMMIT_COUNT_TODAY=$(git log --oneline --since="${TODAY}T00:00:00Z" --until="${TODAY}T23:59:59Z" | wc -l | tr -d ' ')
          if [ "$COMMIT_COUNT_TODAY" -eq 0 ]; then
            COMMIT_COUNT_TODAY=1
          fi
          TOTAL_COMMITS=$(git rev-list --count HEAD 2>/dev/null || echo 1)
          BUILD_NUMBER=$(date -u +%s)
          VERSION="${YY}.${MMDD}.${COMMIT_COUNT_TODAY}"
          VERSION_COMPACT="${YY}${MMDD}${COMMIT_COUNT_TODAY}"
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "version_compact=$VERSION_COMPACT" >> $GITHUB_OUTPUT
          echo "build_number=$BUILD_NUMBER" >> $GITHUB_OUTPUT
          echo "commit_count=$COMMIT_COUNT_TODAY" >> $GITHUB_OUTPUT
          echo "build_date=$(date -u +%Y-%m-%d)" >> $GITHUB_OUTPUT
          echo "=========================================="
          echo "  版本号: $VERSION"
          echo "  紧凑号: $VERSION_COMPACT"
          echo "  构建号: $BUILD_NUMBER"
          echo "  今日提交数: $COMMIT_COUNT_TODAY"
          echo "=========================================="

      - name: Determine Release Type
        id: release_type
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "type=${{ inputs.release_type }}" >> $GITHUB_OUTPUT
          else
            echo "type=dev" >> $GITHUB_OUTPUT
          fi

  # ===== 静态分析门禁 =====
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: ${{ env.FLUTTER_CHANNEL }}
          cache: true

      - name: Install Dependencies
        run: flutter pub get

      # warning/error 一律阻断；lint 提示（info）按存量基线卡数量，只允许减少
      - name: Analyze
        env:
          # 实测 26；留 4 条余量吸收 Linux runner 与本机的平台差异
          ANALYZE_BASELINE: '30'
        run: |
          set -o pipefail
          flutter analyze --no-fatal-infos 2>&1 | tee analyze.log
          COUNT=$(grep -cE '^[[:space:]]+(info|warning|error) -' analyze.log || true)
          echo "当前问题数 ${COUNT} / 基线 ${ANALYZE_BASELINE}"
          if [ "${COUNT}" -gt "${ANALYZE_BASELINE}" ]; then
            echo "::error::静态分析问题数由 ${ANALYZE_BASELINE} 升至 ${COUNT}，请先修复新增项"
            exit 1
          fi

  # ===== Android APK 打包 =====
  build-android:
    name: Build Android
    needs: prepare
    # 标准 2 核 7GB runner，靠 CMake 6 道内存防线（-fno-lto / -fno-inline / 等）避免 OOM
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: ${{ env.JAVA_VERSION }}

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: ${{ env.FLUTTER_CHANNEL }}
          cache: true

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ runner.os }}-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: gradle-${{ runner.os }}-

      - name: Update Version
        run: |
          VERSION="${{ needs.prepare.outputs.version }}"
          sed -i "s/^version:.*/version: ${VERSION}+${{ needs.prepare.outputs.build_number }}/" pubspec.yaml
          grep "^version:" pubspec.yaml

      - name: Remove Aliyun Mirrors
        run: |
          sed -i '/maven.aliyun.com/d' android/settings.gradle.kts
          sed -i '/maven.aliyun.com/d' android/build.gradle.kts

      - name: Install Dependencies
        run: flutter pub get

      - name: Build APK
        run: flutter build apk --release --target-platform=android-arm64 --obfuscate --split-debug-info=build/symbols
        env:
          # 2 核 runner：CMake/Ninja 并行度 = CPU 核数
          CMAKE_BUILD_PARALLEL_LEVEL: 2

      - name: Analyze APK Size
        run: |
          echo "=== APK 内容分析（按大小排序，Top 30）==="
          unzip -l build/app/outputs/flutter-apk/app-release.apk | sort -rn -k1 | head -30
          echo "=== APK 总大小 ==="
          ls -lh build/app/outputs/flutter-apk/app-release.apk

      - name: Rename APK
        id: apk
        run: |
          VERSION="${{ needs.prepare.outputs.version }}"
          TYPE="${{ needs.prepare.outputs.release_type }}"
          BUILD_DATE="${{ needs.prepare.outputs.build_date }}"
          APK_NAME="mr_v${{ needs.prepare.outputs.version_compact }}_${TYPE}_${BUILD_DATE}_arm64.apk"
          cp build/app/outputs/flutter-apk/app-release.apk "${APK_NAME}"
          echo "apk_name=$APK_NAME" >> $GITHUB_OUTPUT
          ls -lh "${APK_NAME}"

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: mr-android-${{ needs.prepare.outputs.version }}
          path: ${{ steps.apk.outputs.apk_name }}
          retention-days: 30

  # ===== iOS 打包（允许失败）=====
  # 修复历史：项目原本用 SPM 模式但 project.pbxproj 没有 Flutter.framework 引用，
  # 导致 Library not loaded: @rpath/Flutter.framework/Flutter 闪退。
  # 现已添加传统 PBXBuildFile 引用 + CI 关闭 SPM 模式确保走传统链接路径。
  build-ios:
    name: Build iOS
    needs: prepare
    runs-on: macos-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: ${{ env.FLUTTER_CHANNEL }}
          cache: true

      - name: Disable Swift Package Manager
        # 项目用传统 Flutter.framework 引用模式（project.pbxproj 已配置 PBXBuildFile）
        # 必须关闭 SPM，否则 Generated.xcconfig 会注入 FLUTTER_FRAMEWORK_SWIFT_PACKAGE_PATH
        # 导致 Xcode 走 SPM 路径但找不到 packageReferences
        run: flutter config --no-enable-swift-package-manager

      - name: Update Version
        run: |
          VERSION="${{ needs.prepare.outputs.version }}"
          sed -i '' "s/^version:.*/version: ${VERSION}+${{ needs.prepare.outputs.build_number }}/" pubspec.yaml
          grep "^version:" pubspec.yaml

      - name: Install Dependencies
        run: flutter pub get

      - name: Verify Build Phases
        # 校验修复是否生效：project.pbxproj 必须包含 Embed Frameworks 和 Thin Binary (embed_and_thin)
        # 这两个 phase 负责 Flutter.framework 的嵌入，缺失会导致 Library not loaded 闪退
        run: |
          echo "=== 检查 project.pbxproj 中的 Embed Frameworks phase ==="
          grep -c "Embed Frameworks" ios/Runner.xcodeproj/project.pbxproj || {
            echo "❌ project.pbxproj 缺少 Embed Frameworks phase！"
            exit 1
          }
          echo "=== 检查 project.pbxproj 中的 Thin Binary phase (embed_and_thin) ==="
          grep -c "embed_and_thin" ios/Runner.xcodeproj/project.pbxproj || {
            echo "❌ project.pbxproj 缺少 embed_and_thin 脚本！Flutter.framework 不会被嵌入"
            exit 1
          }
          echo "✅ build phases 完整"
          echo "=== 检查 Generated.xcconfig 是否还有 SPM 字段 ==="
          if grep -q "FLUTTER_FRAMEWORK_SWIFT_PACKAGE_PATH" ios/Flutter/Generated.xcconfig; then
            echo "⚠️ Generated.xcconfig 仍包含 SPM 字段（可能 flutter config 未生效）"
            grep "FLUTTER_FRAMEWORK_SWIFT_PACKAGE_PATH" ios/Flutter/Generated.xcconfig
          else
            echo "✅ SPM 模式已关闭"
          fi

      - name: Install Pods
        run: |
          cd ios
          rm -rf Pods Podfile.lock
          pod install --repo-update

      - name: Disable Code Signing
        run: |
          cd ios
          # 填入假 Development Team 骗过 Flutter 检查，--no-codesign 会跳过实际签名
          sed -i '' 's/DEVELOPMENT_TEAM = "";/DEVELOPMENT_TEAM = "XXXXXX";/g' Runner.xcodeproj/project.pbxproj

      - name: Build iOS (No Codesign)
        run: flutter build ios --release --no-codesign

      - name: Verify Flutter.framework Embedded
        # 关键校验：Flutter.framework 必须嵌入 Runner.app/Frameworks/，否则启动闪退
        run: |
          echo "=== Runner.app 目录结构 ==="
          ls -lh build/ios/iphoneos/Runner.app/
          echo "=== Runner.app/Frameworks 目录 ==="
          ls -lh build/ios/iphoneos/Runner.app/Frameworks/ 2>/dev/null || {
            echo "❌ Runner.app/Frameworks 不存在！Flutter.framework 未嵌入"
            exit 1
          }
          echo "=== 检查 Flutter.framework 是否嵌入 ==="
          if [ -d "build/ios/iphoneos/Runner.app/Frameworks/Flutter.framework" ]; then
            echo "✅ Flutter.framework 已嵌入"
            ls -lh build/ios/iphoneos/Runner.app/Frameworks/Flutter.framework/
          else
            echo "❌ Flutter.framework 未嵌入到 Runner.app/Frameworks！"
            exit 1
          fi
          echo "=== Runner 二进制大小 ==="
          ls -lh build/ios/iphoneos/Runner.app/Runner
          echo "=== Runner.app 总大小 ==="
          du -sh build/ios/iphoneos/Runner.app

      - name: Create IPA
        id: ipa
        run: |
          VERSION="${{ needs.prepare.outputs.version }}"
          TYPE="${{ needs.prepare.outputs.release_type }}"
          BUILD_DATE="${{ needs.prepare.outputs.build_date }}"
          IPA_NAME="mr_v${{ needs.prepare.outputs.version_compact }}_${TYPE}_${BUILD_DATE}_ios.ipa"
          mkdir -p Payload
          cp -r build/ios/iphoneos/Runner.app Payload/
          zip -r "${IPA_NAME}" Payload
          echo "ipa_name=${IPA_NAME}" >> $GITHUB_OUTPUT
          ls -lh "${IPA_NAME}"

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: mr-ios-${{ needs.prepare.outputs.version }}
          path: ${{ steps.ipa.outputs.ipa_name }}
          retention-days: 30

  # ===== 鸿蒙 HarmonyOS 打包（允许失败）=====
  # 鸿蒙 Flutter SDK 为华为 fork（https://gitcode.com/openharmony-sig/flutter_flutter）
  # Command Line Tools 从华为云公开镜像下载（无需登录）
  # 完整包 2.0 GiB，包含 Node.js 18 + hvigorw + ohpm + SDK + hdc
  # 镜像地址：https://repo.huaweicloud.com/harmonyos/ohpm/5.1.0/
  build-ohos:
    name: Build HarmonyOS
    needs: prepare
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4

      - name: Setup JDK 17
        # 官方文档要求 JDK 17（签名工具 hap-sign-tool.jar 需要 Java）
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Download & Setup Command Line Tools
        # 从华为云公开镜像下载完整 Command Line Tools（2.0 GiB，无需登录）
        # 包含 Node.js 18 + hvigorw + ohpm + SDK + hdc
        run: |
          set -e
          CLI_URL="https://repo.huaweicloud.com/harmonyos/ohpm/5.1.0/commandline-tools-linux-x64-5.1.0.840.zip"
          echo "=== 下载 Command Line Tools (2.0 GiB) ==="
          wget -q "$CLI_URL" -O /tmp/commandline-tools.zip
          FILE_SIZE=$(stat -c%s /tmp/commandline-tools.zip)
          echo "下载文件大小: $((FILE_SIZE / 1024 / 1024)) MB"
          echo "=== 解压到 /opt ==="
          unzip -q /tmp/commandline-tools.zip -d /opt/
          rm /tmp/commandline-tools.zip
          echo "=== /opt 目录结构 ==="
          find /opt -maxdepth 3 -type d | head -30
          # 查找 command-line-tools 目录
          CMD_DIR=$(find /opt -maxdepth 2 -name "command-line-tools" -type d | head -1)
          if [ -z "$CMD_DIR" ]; then
            CMD_DIR=$(find /opt -maxdepth 2 -name "bin" -type d | head -1 | xargs dirname)
          fi
          echo "CMD_DIR: $CMD_DIR"
          # 设置环境变量（按官方文档）
          export NODE_HOME=${CMD_DIR}/tool/node
          export HDC_HOME=${CMD_DIR}/sdk/default/openharmony/toolchains
          echo "NODE_HOME=${NODE_HOME}" >> $GITHUB_ENV
          echo "HDC_HOME=${HDC_HOME}" >> $GITHUB_ENV
          echo "${CMD_DIR}/bin" >> $GITHUB_PATH
          echo "${NODE_HOME}/bin" >> $GITHUB_PATH
          echo "${HDC_HOME}" >> $GITHUB_PATH
          # 验证工具链（$GITHUB_PATH 在同一步骤不生效，用完整路径）
          echo "=== 验证工具链 ==="
          ${CMD_DIR}/tool/node/bin/node -v
          ${CMD_DIR}/bin/hvigorw -v
          ${CMD_DIR}/bin/ohpm -v
          java -version

      - name: Configure npm & ohpm Registry
        # 按官方文档配置 npm 和 ohpm 镜像仓库
        # 增加 npm 网络重试配置：hvigorw 内部会 npm install pnpm，
        # 华为云镜像偶发 ECONNRESET 导致构建直接失败
        run: |
          npm config set registry https://repo.huaweicloud.com/repository/npm/
          npm config set @ohos:registry https://repo.harmonyos.com/npm/
          npm config set fetch-retries 5
          npm config set fetch-retry-mintimeout 20000
          npm config set fetch-retry-maxtimeout 120000
          npm config set fetch-timeout 300000
          ohpm config set registry https://ohpm.openharmony.cn/ohpm/
          ohpm config set strict_ssl false
          echo "=== .npmrc ==="
          cat ~/.npmrc

      - name: Pre-install pnpm
        # hvigorw 内部会执行 npm install pnpm@8.13.1，预装避免构建时网络抖动导致失败
        # 用 --fetch-retries 重试，ECONNRESET 是华为云镜像的偶发问题
        run: |
          echo "=== 预装 pnpm@8.13.1 ==="
          npm install -g pnpm@8.13.1 2>&1 || {
            echo "⚠️ pnpm 预装失败，hvigorw 会自行重试"
          }
          pnpm -v 2>/dev/null && echo "✅ pnpm 已就绪" || echo "⚠️ pnpm 未就绪"

      - name: Clone HarmonyOS Flutter SDK
        # 华为 fork 的 Flutter SDK，动态获取最高 release 版本分支
        # 仓库：https://gitcode.com/openharmony-sig/flutter_flutter
        # 分支命名规则：oh-<Flutter版本>-release（如 oh-3.41.9-release）
        # 本地用 Flutter 3.41.7，云端鸿蒙 fork 最高 release 为 3.41.9，版本对齐
        run: |
          # 动态获取所有 oh-*-release 分支，选版本号最高的
          LATEST_RELEASE=$(git ls-remote --heads https://gitcode.com/openharmony-sig/flutter_flutter.git \
            | sed -n 's|.*refs/heads/oh-\([0-9.]*\)-release|\1|p' \
            | sort -V \
            | tail -1)
          if [ -z "$LATEST_RELEASE" ]; then
            echo "❌ 未找到 oh-*-release 分支"
            exit 1
          fi
          BRANCH="oh-${LATEST_RELEASE}-release"
          echo "=== 最新 release 分支: $BRANCH (Flutter $LATEST_RELEASE) ==="
          git clone --depth 1 --branch "$BRANCH" https://gitcode.com/openharmony-sig/flutter_flutter.git ~/ohos-flutter
          echo "$HOME/ohos-flutter/bin" >> $GITHUB_PATH
          # 浅克隆丢失 git tag，手动打 tag 让 git describe --tags 返回正确版本
          # 否则 Flutter 报告 0.0.0-unknown，pub 依赖求解失败
          cd ~/ohos-flutter
          git tag "$LATEST_RELEASE"
          rm -f bin/cache/flutter.version.json
          export PATH="$HOME/ohos-flutter/bin:$PATH"
          echo "=== flutter --version ==="
          flutter --version
          # 提取 Dart 版本供后续 Patch 步骤使用
          DART_VERSION=$(flutter --version | grep -oP 'Dart \K[0-9.]+')
          echo "OHOS_FLUTTER_VERSION=$LATEST_RELEASE" >> $GITHUB_ENV
          echo "OHOS_DART_VERSION=$DART_VERSION" >> $GITHUB_ENV
          echo "=== 鸿蒙 Flutter $LATEST_RELEASE / Dart $DART_VERSION ==="

      - name: Patch pubspec for HarmonyOS
        # 鸿蒙 Flutter SDK 可能与项目 Dart SDK 约束不兼容，动态放宽到当前版本
        # 同时删除 flutter.config 节点（Flutter 3.45+ 新增字段，旧版不识别）
        # 在 Clone 之后执行（需要 OHOS_DART_VERSION 环境变量），
        # 在 Enable/Generate/pub get 之前执行（这些命令会解析 pubspec.yaml）
        run: |
          # 动态放宽 Dart SDK 约束到当前鸿蒙 Flutter 的 Dart 版本
          DART_VERSION="${{ env.OHOS_DART_VERSION }}"
          if [ -n "$DART_VERSION" ]; then
            sed -i "s/sdk: '>=3.12.0 <4.0.0'/sdk: '>=${DART_VERSION} <4.0.0'/" pubspec.yaml
            echo "=== 放宽 Dart SDK 约束到 >=${DART_VERSION} ==="
          else
            echo "⚠️ 未获取到 Dart 版本，跳过 SDK 约束放宽"
          fi
          grep "sdk:" pubspec.yaml
          python3 << 'PYEOF'
          import re
          with open('pubspec.yaml', 'r', encoding='utf-8') as f:
              content = f.read()
          # 删除 flutter: 下的 config: 节点（含其所有缩进更深的子行）
          new_content = re.sub(
              r'^  config:\n(?:    .*\n)+',
              '',
              content,
              flags=re.MULTILINE
          )
          if new_content == content:
              print('⚠️ 未找到 flutter.config 节点，可能已被移除')
          else:
              print('✅ 已移除 flutter.config 节点（SPM 配置，鸿蒙 Flutter 可能不识别）')
          with open('pubspec.yaml', 'w', encoding='utf-8') as f:
              f.write(new_content)
          PYEOF
          echo "=== 打补丁后的 flutter 节点 ==="
          sed -n '/^flutter:/,/^[a-z]/p' pubspec.yaml

      - name: Enable HarmonyOS Platform
        run: |
          export PATH="$HOME/ohos-flutter/bin:$PATH"
          flutter --version
          flutter config --enable-ohos

      - name: Generate ohos Project
        # CI 中用华为 fork Flutter 生成 ohos 工程骨架
        # 不签名：生成未签名 HAP，用户自行用 DevEco Studio 签名后安装
        run: |
          export PATH="$HOME/ohos-flutter/bin:$PATH"
          flutter create --platforms ohos .
          echo "=== ohos 目录结构 ==="
          ls -la ohos/
          echo "=== 生成的 build-profile.json5 ==="
          cat ohos/build-profile.json5

      - name: Inject QuickJS C Build
        # 将鸿蒙版 CMakeLists.txt 和 version-script 注入 ohos 工程
        # CMakeLists.txt 引用项目根 quickjs/ 源码，编译 libquickjs_c_bridge.so
        # dart:ffi 通过 DynamicLibrary.open('libquickjs_c_bridge.so') 加载
        run: |
          mkdir -p ohos/entry/src/main/cpp
          cp quickjs/ohos/CMakeLists.txt ohos/entry/src/main/cpp/CMakeLists.txt
          cp android/app/src/main/cpp/quickjs_bridge.map ohos/entry/src/main/cpp/quickjs_bridge.map
          echo "=== cpp 目录 ==="
          ls -la ohos/entry/src/main/cpp/

      - name: Configure CMake in build-profile
        # 直接覆盖 ohos/entry/build-profile.json5，注入 externalNativeOptions 到 buildOption 内
        # 让 hvigorw 调用 CMake 编译 libquickjs_c_bridge.so
        run: |
          cat > ohos/entry/build-profile.json5 << 'EOF'
          {
            "apiType": "stageMode",
            "buildOption": {
              "externalNativeOptions": {
                "path": "./src/main/cpp/CMakeLists.txt",
                "arguments": "",
                "cppFlags": "",
                "abiFilters": ["arm64-v8a"]
              }
            },
            "targets": [
              {
                "name": "default",
                "runtimeOS": "HarmonyOS"
              },
              {
                "name": "ohosTest"
              }
            ]
          }
          EOF
          echo "=== build-profile.json5 ==="
          cat ohos/entry/build-profile.json5

      - name: Configure ohos Permissions
        # 在 module.json5 加网络权限（书源 HTTP 请求需要）
        run: |
          python3 << 'PYEOF'
          import re, sys
          path = 'ohos/entry/src/main/module.json5'
          with open(path, 'r', encoding='utf-8') as f:
              content = f.read()
          if 'ohos.permission.INTERNET' in content:
              print('⚠️ 网络权限已存在，跳过')
              sys.exit(0)
          perms = '''"requestPermissions": [
              { "name": "ohos.permission.INTERNET" },
              { "name": "ohos.permission.GET_NETWORK_INFO" }
            ],'''
          if '"requestPermissions"' in content:
              # 已有数组，在数组开头追加权限
              new_content = re.sub(
                  r'("requestPermissions"\s*:\s*\[)',
                  r'\1\n            { "name": "ohos.permission.INTERNET" },\n            { "name": "ohos.permission.GET_NETWORK_INFO" },',
                  content, count=1)
          else:
              # 无数组，在 "module": { 后插入
              new_content = re.sub(r'("module"\s*:\s*\{)', r'\1\n    ' + perms, content, count=1)
          if new_content == content:
              print('⚠️ 注入失败，module.json5 结构未识别')
              sys.exit(1)
          with open(path, 'w', encoding='utf-8') as f:
              f.write(new_content)
          print('✅ 已注入网络权限')
          PYEOF
          echo "=== module.json5 ==="
          cat ohos/entry/src/main/module.json5

      - name: Update Version
        run: |
          export PATH="$HOME/ohos-flutter/bin:$PATH"
          VERSION="${{ needs.prepare.outputs.version }}"
          sed -i "s/^version:.*/version: ${VERSION}+${{ needs.prepare.outputs.build_number }}/" pubspec.yaml
          grep "^version:" pubspec.yaml

      - name: Install Dependencies
        run: |
          export PATH="$HOME/ohos-flutter/bin:$PATH"
          flutter pub get

      - name: Install ohpm Dependencies
        # 预装 ohpm 依赖（主要是 hypium 测试框架）
        # 注意：flutter_ohos 包由 hvigorw 构建时动态拉取，ohpm install 不会下载它
        run: |
          cd ohos
          ohpm install
          echo "=== oh_modules 目录结构（顶层）==="
          find oh_modules -maxdepth 3 -type d 2>/dev/null | head -30

      - name: Build HAP
        # 不签名构建：生成未签名 HAP，用户自行用 DevEco Studio 签名后安装
        # 多阶段构建策略（最多 3 次）：
        #   1. 第一次构建：hvigorw 内部 ohpm install 下载 flutter_ohos，ArkTS 编译失败
        #      - 也可能因网络抖动（ECONNRESET）在 pnpm 安装阶段失败 → 直接重试
        #   2. patch flutter_ohos API 不兼容后重新构建
        #   3. 如仍失败，最后一次兜底
        run: |
          export PATH="$HOME/ohos-flutter/bin:$PATH"

          # patch 函数：修复 flutter_ohos 与 HarmonyOS 5.1 SDK 的 API 不兼容
          patch_flutter_ohos() {
            KEH=$(find ohos/oh_modules -type f -name "KeyEventHandler.ets" 2>/dev/null | head -1)
            PIP=$(find ohos/oh_modules -type f -name "PiPVisibilityBridge.ets" 2>/dev/null | head -1)
            if [ -n "$KEH" ]; then
              # event.isCapsLockOn / isNumLockOn → false（替换整个属性访问）
              sed -i 's/[a-zA-Z_]\+\.isCapsLockOn/false/g' "$KEH"
              sed -i 's/[a-zA-Z_]\+\.isNumLockOn/false/g' "$KEH"
              echo "✅ Patched KeyEventHandler.ets"
            else
              echo "⚠️ KeyEventHandler.ets 未找到"
            fi
            if [ -n "$PIP" ]; then
              # window.getGlobalWindowMode() → 1，window.GlobalWindowMode → number
              sed -i 's/window\.getGlobalWindowMode()/1/g' "$PIP"
              sed -i 's/window\.GlobalWindowMode\.[A-Z_]\+/1/g' "$PIP"
              sed -i 's/window\.GlobalWindowMode/number/g' "$PIP"
              echo "✅ Patched PiPVisibilityBridge.ets"
            else
              echo "⚠️ PiPVisibilityBridge.ets 未找到"
            fi
          }

          BUILD_EXIT=1
          for ATTEMPT in 1 2 3; do
            echo "=== 构建尝试 $ATTEMPT/3 ==="
            set +e
            flutter build hap --release --verbose > /tmp/hap-build-${ATTEMPT}.log 2>&1
            BUILD_EXIT=$?
            set -e
            echo "=== 尝试 $ATTEMPT 退出码: $BUILD_EXIT ==="

            if [ $BUILD_EXIT -eq 0 ]; then
              break
            fi

            # 判断失败原因：flutter_ohos 是否已下载到 oh_modules
            HAS_FLUTTER_OHOS=$(find ohos/oh_modules -type f -name "KeyEventHandler.ets" 2>/dev/null | head -1)

            if [ -n "$HAS_FLUTTER_OHOS" ]; then
              # flutter_ohos 已下载 → ArkTS 编译错误 → patch 后重试
              echo "=== flutter_ohos 已下载，执行 patch ==="
              patch_flutter_ohos
            else
              # flutter_ohos 未下载 → 网络错误（pnpm/ohpm install 失败）→ 直接重试
              echo "=== flutter_ohos 未下载（可能网络错误），直接重试 ==="
              grep -i "ECONNRESET\|ETIMEDOUT\|fetch failed\|Operation Error\|execute failed" /tmp/hap-build-${ATTEMPT}.log | head -10 || true
            fi
          done

          # 最终结果处理
          echo "=== 列出所有 .hap 文件 ==="
          find . -name "*.hap" -type f 2>/dev/null || echo "未找到任何 .hap 文件"

          if [ $BUILD_EXIT -ne 0 ]; then
            echo "=== 错误上下文 ==="
            LAST_LOG=/tmp/hap-build-${ATTEMPT}.log
            if [ -f "$LAST_LOG" ]; then
              grep -B 5 -A 10 "error|Error|FAILED|Exception|Cause:" "$LAST_LOG" || true
            fi
            # 即使构建失败，如果有 unsigned HAP 产物也继续上传
            UNSIGNED_HAP=$(find . -name "*-unsigned.hap" -type f 2>/dev/null | head -1)
            if [ -n "$UNSIGNED_HAP" ]; then
              echo "✅ 构建失败但有 unsigned HAP 产物: $UNSIGNED_HAP"
              BUILD_EXIT=0
            else
              echo "=== 完整构建日志（最后一次）==="
              cat "$LAST_LOG" 2>/dev/null || cat /tmp/hap-build-1.log
            fi
          fi
          exit $BUILD_EXIT

      - name: Rename HAP
        id: hap
        run: |
          VERSION="${{ needs.prepare.outputs.version }}"
          TYPE="${{ needs.prepare.outputs.release_type }}"
          BUILD_DATE="${{ needs.prepare.outputs.build_date }}"
          HAP_NAME="mr_v${{ needs.prepare.outputs.version_compact }}_${TYPE}_${BUILD_DATE}_ohos.hap"
          # 兼容不同 SDK 版本的产物路径
          # 优先选择 signed HAP（手动签名产物），其次排除 unsigned，最后兜底 unsigned
          HAP_SRC=$(find . -name "*-signed.hap" -type f 2>/dev/null | head -1)
          if [ -z "$HAP_SRC" ]; then
            HAP_SRC=$(find . -name "*.hap" -type f ! -name "*-unsigned.hap" 2>/dev/null | head -1)
          fi
          if [ -z "$HAP_SRC" ]; then
            HAP_SRC=$(find . -name "*-unsigned.hap" -type f 2>/dev/null | head -1)
          fi
          if [ -z "$HAP_SRC" ]; then
            echo "❌ 未找到 .hap 产物"
            find . -name "*.hap" -type f 2>/dev/null | head -20
            exit 1
          fi
          cp "$HAP_SRC" "${HAP_NAME}"
          echo "hap_name=${HAP_NAME}" >> $GITHUB_OUTPUT
          ls -lh "${HAP_NAME}"

      - name: Upload Artifacts
        if: steps.hap.outputs.hap_name != ''
        uses: actions/upload-artifact@v4
        with:
          name: mr-ohos-${{ needs.prepare.outputs.version }}
          path: ${{ steps.hap.outputs.hap_name }}
          retention-days: 30

  # ===== 合并发布（等待所有平台，无论成功失败）=====
  release:
    name: Create Release
    needs: [prepare, build-android, build-ios, build-ohos]
    runs-on: ubuntu-latest
    # always() 会无视上游 skip，PR 上必须显式排除，否则会在没有产物时空跑发布
    if: always() && github.event_name != 'pull_request'
    steps:
      - uses: actions/checkout@v4

      - name: Check Build Results
        id: results
        run: |
          echo "android_result=${{ needs.build-android.result }}" >> $GITHUB_OUTPUT
          echo "ios_result=${{ needs.build-ios.result }}" >> $GITHUB_OUTPUT
          echo "ohos_result=${{ needs.build-ohos.result }}" >> $GITHUB_OUTPUT
          echo "=== 构建结果 ==="
          echo "Android: ${{ needs.build-android.result }}"
          echo "iOS: ${{ needs.build-ios.result }}"
          echo "HarmonyOS: ${{ needs.build-ohos.result }}"

      - name: Download Android Artifact
        if: needs.build-android.result == 'success'
        uses: actions/download-artifact@v4
        with:
          name: mr-android-${{ needs.prepare.outputs.version }}
          path: artifacts/android

      - name: Download iOS Artifact
        if: needs.build-ios.result == 'success'
        uses: actions/download-artifact@v4
        with:
          name: mr-ios-${{ needs.prepare.outputs.version }}
          path: artifacts/ios

      - name: Download HarmonyOS Artifact
        if: needs.build-ohos.result == 'success'
        uses: actions/download-artifact@v4
        with:
          name: mr-ohos-${{ needs.prepare.outputs.version }}
          path: artifacts/ohos

      - name: List Artifacts
        run: |
          echo "=== Android ==="
          ls -lh artifacts/android/ 2>/dev/null || echo "无 Android 产物"
          echo "=== iOS ==="
          ls -lh artifacts/ios/ 2>/dev/null || echo "无 iOS 产物"
          echo "=== HarmonyOS ==="
          ls -lh artifacts/ohos/ 2>/dev/null || echo "无 HarmonyOS 产物"

      - name: Build Release Body
        id: body
        run: |
          VERSION="${{ needs.prepare.outputs.version }}"
          VERSION_COMPACT="${{ needs.prepare.outputs.version_compact }}"
          TYPE="${{ needs.prepare.outputs.release_type }}"
          BUILD_DATE="${{ needs.prepare.outputs.build_date }}"
          COMMIT_COUNT="${{ needs.prepare.outputs.commit_count }}"

          TYPE_LABEL="测试版 (dev)"
          if [ "$TYPE" = "stable" ]; then
            TYPE_LABEL="正式版 (stable)"
          fi

          BODY="## MR ${VERSION_COMPACT}\n\n"
          BODY="${BODY}**打包类型**: ${TYPE_LABEL}\n\n"
          BODY="${BODY}**构建日期**: ${BUILD_DATE}\n\n"
          BODY="${BODY}**版本号**: ${VERSION}（pubspec.yaml 格式）\n\n"
          BODY="${BODY}**构建号**: ${{ needs.prepare.outputs.build_number }}\n\n"
          BODY="${BODY}---\n\n"

          # Android 下载说明
          if [ "${{ needs.build-android.result }}" = "success" ]; then
            BODY="${BODY}### Android\n"
            BODY="${BODY}- \`.apk\` 文件直接安装\n"
            BODY="${BODY}- \`.zip\` 文件为压缩包，解压后安装\n"
            BODY="${BODY}- 安装方法：下载 APK → 允许未知来源 → 安装\n\n"
          else
            BODY="${BODY}### Android\n❌ 打包失败，请查看 Actions 日志\n\n"
          fi

          # iOS 下载说明
          if [ "${{ needs.build-ios.result }}" = "success" ]; then
            BODY="${BODY}### iOS\n"
            BODY="${BODY}- \`.ipa\` 文件需通过侧载工具安装（AltStore/Sideloadly/TrollStore）\n"
            BODY="${BODY}- 未签名版本，需自签或使用侧载工具\n"
            BODY="${BODY}- 安装方法：下载 IPA → 使用侧载工具安装\n\n"
          else
            BODY="${BODY}### iOS\n⚠️ 打包失败或暂不支持，请查看 Actions 日志\n\n"
          fi

          # 鸿蒙 HarmonyOS 下载说明
          if [ "${{ needs.build-ohos.result }}" = "success" ]; then
            BODY="${BODY}### HarmonyOS（鸿蒙）\n"
            BODY="${BODY}- \`.hap\` 文件为鸿蒙安装包\n"
            BODY="${BODY}- 安装方法：通过 hdc 工具安装 \`hdc install xxx.hap\`，或推送到设备后用文件管理器安装\n"
            BODY="${BODY}- 需在鸿蒙系统设置中允许未知来源应用安装\n\n"
          else
            BODY="${BODY}### HarmonyOS（鸿蒙）\n⚠️ 打包失败或暂不支持，请查看 Actions 日志\n"
            BODY="${BODY}（鸿蒙 Flutter SDK 当前基于 3.27.4，与项目 Dart 要求可能不兼容；免账号 CLI 包可能缺 SDK）\n\n"
          fi

          # 写入文件避免转义问题
          echo -e "$BODY" > release_body.md
          cat release_body.md

      - name: Create Release
        # 用 gh CLI 替代 softprops/action-gh-release，内置 3 次重试 + 30s 间隔
        # 原因：GitHub API 偶发 503 Unicorn 错误（服务器临时故障），原 action 无重试机制
        # 重试时若 release 已存在（上次创建成功但上传文件失败），自动切换为 upload 模式
        run: |
          TAG="v${{ needs.prepare.outputs.version_compact }}-${{ needs.prepare.outputs.release_type }}"
          TITLE="MR ${{ needs.prepare.outputs.version_compact }} (${{ needs.prepare.outputs.release_type == 'stable' && '正式版' || '测试版' }})"

          FLAGS=""
          if [ "${{ needs.prepare.outputs.release_type }}" != "stable" ]; then
            FLAGS="--prerelease"
          fi

          # 收集所有产物文件
          FILES=""
          for f in artifacts/android/* artifacts/ios/* artifacts/ohos/*; do
            [ -f "$f" ] && FILES="$FILES $f"
          done

          if [ -z "$FILES" ]; then
            echo "❌ No artifacts found, skipping release"
            exit 1
          fi

          echo "=== Release target: $TAG ==="
          echo "=== Files: $FILES ==="

          for i in 1 2 3; do
            echo "=== Attempt $i/3 ==="

            # 尝试创建 release（含上传文件）
            if gh release create "$TAG" \
              --title "$TITLE" \
              --notes-file release_body.md \
              $FLAGS \
              $FILES; then
              echo "✅ Release created successfully"
              exit 0
            fi

            echo "⚠️ Create failed, checking if release already exists..."

            # 如果 release 已存在（上次创建成功但上传文件失败），尝试补传文件
            if gh release view "$TAG" > /dev/null 2>&1; then
              echo "Release exists, uploading assets..."
              if gh release upload "$TAG" $FILES --clobber; then
                echo "✅ Assets uploaded successfully"
                exit 0
              fi
            fi

            if [ $i -lt 3 ]; then
              echo "⏳ Waiting 30s before retry..."
              sleep 30
            fi
          done

          echo "❌ All 3 attempts failed"
          exit 1
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
